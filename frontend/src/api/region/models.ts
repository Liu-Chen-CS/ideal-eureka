export type RegionDivisionVO = {
    regionId: number;
    regionDistribution: string,
    customer: string,
    listName: string,
    division: string,
    createdOn: string,
    restmenge:string,
    version:number,
}

export type RegionAPIReponse = {
    data: any;
};